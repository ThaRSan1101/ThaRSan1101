Your CV placeholder

Replace this file with your actual CV PDF or rename and update the link in `index.html`.

To link a PDF instead, add it to this repo (for example `assets/Tharsan-CV.pdf`) and update the download href in `index.html`.
